.venv/
build/
__pycache__/
*.pyc
data/raw_vqa/
.venv-train/
checkpoints/

# run artifacts (Hydra outputs, local wandb run dirs, scratch) -- corralled under one dir;
# real wandb data lives on the server (WANDB_DIR=/hdd2/ahmedaly/echogrpo/wandb), this is
# just whatever local debug copy a run leaves behind
runs/
.wandb/
.venv-vllm/
